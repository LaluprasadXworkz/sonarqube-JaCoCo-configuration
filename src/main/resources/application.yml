spring:
  application:
    name: sonarqube-jacoco-configuration

  datasource:
    url: jdbc:mysql://192.168.1.100:3306/sonarqube_db?useSSL=false&serverTimezone=UTC
    username: root
    password: xworkzodc
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8081
